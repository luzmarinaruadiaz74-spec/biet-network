{
  "name": "@biet-network/contracts",
  "version": "0.2.10",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "deploy:base": "forge script script/Deploy.s.sol --rpc-url base --broadcast --verify",
    "deploy:base-testnet": "forge script script/Deploy.s.sol --rpc-url base-sepolia --broadcast",
    "clean": "forge clean",
    "lint": "solhint 'src/**/*.sol'"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "@openzeppelin/contracts-upgradeable": "^5.0.0",
    "solhint": "^4.0.0"
  }
}